export enum ProfileTypes {
  DEFAULT = 'default',
  ADMIN = 'admin',
}

export enum ApplicationStatus {
  PENDING = 'pending',
  REJECTED = 'rejected',
  APPROVED = 'approved',
  COMPLATED = 'completed',
}

export enum StatusUpdatedBy {
  ADMIN = 'admin',
  MENTOR = 'mentor',
}
