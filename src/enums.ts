export enum ProfileTypes {
  DEFAULT = 'default',
  ADMIN = 'admin',
}

export enum ApplicationStatus {
  PENDING = 'pending',
  REJECTED = 'rejected',
  APPROVED = 'approved',
}

export enum StatusUpdatedBy {
  ADMIN = 'admin',
  MENTOR = 'mentor',
}
